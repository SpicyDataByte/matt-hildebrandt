//null - lack of value

int? age = null; //use ? to signify variable can be nullable
string firstname = "";
// string is nullable by default
Console.WriteLine(age);


Console.WriteLine(firstname + 2);
Console.ReadLine();
