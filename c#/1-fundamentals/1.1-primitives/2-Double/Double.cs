
// doubles can have a decimal point 
// some rounding that happens something like after 16 digits

double averageAge;

// However if you only use ints it will will imply ints in your result
averageAge = (43 + 21 + 62) / 3;

Console.WriteLine(averageAge);